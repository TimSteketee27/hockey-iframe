# API-cache en device-token (cache/hapi_device.json) — nooit committen
# (alleen de .htaccess die de map afschermt gaat mee)
cache/*
!cache/.htaccess

# Lokale Claude Code-instellingen
.claude/settings.local.json

# OS / editor
Thumbs.db
.DS_Store
